#ifndef ENUMS_H
#define ENUMS_H

#include <QObject>

enum DataRole {
    IsNewRole = Qt::UserRole + 1    // Enum-Wert für eine neue Rolle, basiert auf Qt::UserRole(defaut wert ist 32)
};

#endif // ENUMS_H
